# 🚀 Calculator AI — 5 分钟部署指南

> 给同学的电脑上部署，**不需要装任何开发工具**，只需要 VS Code。

---

## 方式一：直接安装（1 分钟 ⭐推荐）

### 你需要的东西
- ✅ VS Code 已安装
- ✅ `calculator-ai-1.0.0.vsix` 文件（就在这个文件夹里）
- ✅ DeepSeek API Key（如果没有，首次使用时会弹出输入框让你输入）

### 步骤

**第 1 步**：打开 VS Code

**第 2 步**：按 `Ctrl+Shift+P`

**第 3 步**：输入 `vsix`，选择 `Extensions: Install from VSIX...`

**第 4 步**：选择本文件夹里的 `calculator-ai-1.0.0.vsix`

**第 5 步**：看到右下角提示「扩展安装成功」即可

**第 6 步**：`Ctrl+Shift+P` → 输入 `Calculator AI` → 回车，计算器就出来了！

---

## 方式二：从源码构建（如果你要改代码）

### 前提
- 安装 Node.js：https://nodejs.org （下载 LTS 版，一路 Next）

### 步骤

打开终端（PowerShell 或 Git Bash），逐行执行：

```bash
# 1. 进入项目目录
cd 这个文件夹的路径

# 2. 安装依赖（只需一次）
npm install

# 3. 编译
npx tsc -p ./

# 4. 打包成 .vsix
npm install -g @vscode/vsce
npx @vscode/vsce package --allow-missing-repository

# 5. 安装
code --install-extension calculator-ai-1.0.0.vsix --force
```

---

## 如何获取 DeepSeek API Key？

1. 打开 https://platform.deepseek.com
2. 注册账号（用手机号或邮箱）
3. 进入「API Keys」→「创建 API Key」
4. 复制保存（格式：`sk-xxxxx`）
5. 打开计算器，首次使用 AI 功能时会弹出输入框，粘贴进去即可

> 💡 新用户通常有免费额度

---

## 验证是否成功

打开计算器后，在右侧对话框输入：
```
1 + 1 等于多少
```

如果 AI 回复了计算步骤和结果，说明部署成功！✅
